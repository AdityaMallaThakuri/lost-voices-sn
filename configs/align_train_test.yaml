mode: train

corpus_dir: data/processed/audio/mfa_corpus_test
dictionary_path: data/processed/audio/mfa_dict.dict
output_model_path: models/sunuwar_acoustic_model_TEST.zip
output_textgrid_dir: data/processed/audio/textgrids_test

# Small smoke-test subset (1CO chapters 1-3, 113 segments) to validate the
# pipeline mechanics (corpus format, dictionary coverage, MFA command,
# TextGrid output) before committing to the multi-day full-corpus run.
num_jobs: 4
clean: true
temporary_directory: D:/mfa_temp_test
