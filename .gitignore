# Raw data — CC BY-NC-ND 4.0, do not redistribute
data/raw/suzBl_usfm.zip
data/raw/suzBl_readaloud.zip
data/raw/supplementary/

# Large model files
models/*.bin
models/*.pt
models/*.vec
models/*.npy
models/tts_checkpoints/

# Gensim fastText auxiliary files
*.wv.vectors_ngrams.npy
*.wv.vectors_vocab.npy
*.trainables.syn1neg.npy

new_segments/



# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/

# Misc
.DS_Store
*.log
wandb/


*.vec




# Audio files — too large for GitHub; WAVs and pairs are generated outputs
data/raw/audio/
data/processed/audio/

# TTS dataset — the .wav rule below already excludes the audio, but the
# metadata.csv files carry the Sunuwar Bible transcripts themselves, which are
# CC BY-NC-ND and must not be redistributed. Exclude the whole directory.
data/processed/tts_dataset/

# Dataset zip(s) staged for upload to Drive — licensed audio + transcripts.
# Glob covers size-stamped variants too (e.g. tts_dataset_1.37h.zip).
tts_dataset*.zip

# Trained TTS checkpoints — model.safetensors exceeds GitHub's 100MB file
# limit. Keep them in Drive or push to the HF Hub, not here.
models/tts/
*.safetensors
*.mp3
*.wav
*.mp4

SUZWBTN1DA/

# Local scratch for moving data to Colab/Drive — never meant for git
_colab_transfer/

sunuwar_resources/